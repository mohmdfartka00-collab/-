# Cloudflare Worker URL (Proxy target)
CLOUDFLARE_WORKER_URL=https://divine-firefly-7938.mohmdfartka00.workers.dev
# Cloudflare Worker Admin Token (Secret used ONLY on the server side between Proxy and Worker)
CLOUDFLARE_ADMIN_TOKEN=

# Firebase Service Account JSON (Required for secure server-side token verification)
# Format: {"type": "service_account", "project_id": "...", ...}
FIREBASE_SERVICE_ACCOUNT=
